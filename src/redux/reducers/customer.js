const initialState = {};

const customerReducer = (state = initialState, action) => {
    switch (action.type) {
    //     case SOMETHING:
    //         return { }
        default:
            return state;
    }
};

export default customerReducer;
