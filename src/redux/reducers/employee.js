const initialState = {};

const employeeReducer = (state = initialState, action) => {
    switch (action.type) {
    //     case SOMETHING:
    //         return { }
        default:
            return state;
    }
}

export default employeeReducer;