const initialState = {};

const adminReducer = (state = initialState, action) => {
    switch (action.type) {
    //     case SOMETHING:
    //         return { }
        default:
            return state;
    }
}

export default adminReducer;