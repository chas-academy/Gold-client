const initialState = {};

const authReducer = (state = initialState, action) => {
    switch (action.type) {
    //     case SOMETHING:
    //         return { }
        default:
            return state;
    }
}

export default authReducer;